# API-recon harness — LLM config (model selection stays config-driven, per
# north_stars LLM-2). Model must be a text model from allowed_models.csv
# (project root). See llm_rules.md for token/temperature/reasoning rules.

llm:
  model_name: "gpt-5.5"
  max_tokens: 8000
  temperature: 1.0
  reasoning_effort: "none"
